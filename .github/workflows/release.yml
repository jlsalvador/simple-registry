name: CI/CD Pipeline

on:
  push:
  pull_request:

permissions:
  contents: write
  packages: write

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          lfs: true

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: "go.mod"
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Run tests (cyclo)
        id: test-cyclo
        run: make test-cyclo 2>&1 | tee test-cyclo.log
        continue-on-error: true

      - name: Run tests (misspell)
        id: test-misspell
        run: make test-misspell 2>&1 | tee test-misspell.log
        continue-on-error: true

      - name: Run tests (go test)
        id: test-go
        run: make test-go 2>&1 | tee test-go.log
        continue-on-error: true

      - name: Generate coverage reports
        if: always()
        run: make cover

      - name: Start registry server
        run: |
          go run ./cmd/simple-registry serve \
            -datadir /tmp/registry \
            -adminname admin \
            -adminpwd testpassword123 \
            > /tmp/registry.log 2>&1 &

          echo $! > /tmp/registry.pid

          echo "Waiting for registry to be ready..."
          max_attempts=30
          attempt=0
          while [ $attempt -lt $max_attempts ]; do
            if curl -u admin:testpassword123 -sf http://localhost:5000/v2/ >/dev/null 2>&1; then
              echo "✅ Registry is ready!"
              break
            fi
            attempt=$((attempt + 1))
            if [ $attempt -eq $max_attempts ]; then
              echo "❌ Registry failed to start after $max_attempts attempts"
              echo "=== Registry Logs ==="
              cat /tmp/registry.log
              exit 1
            fi
            echo "  Attempt $attempt/$max_attempts..."
            sleep 1
          done

      - name: Run OCI Distribution Spec conformance tests
        id: test-oci
        continue-on-error: true
        uses: opencontainers/distribution-spec@v1.1.1
        env:
          OCI_ROOT_URL: http://localhost:5000
          OCI_USERNAME: admin
          OCI_PASSWORD: testpassword123
          OCI_NAMESPACE: library/conformance-test
          OCI_CROSSMOUNT_NAMESPACE: myorg/other
          OCI_TEST_PULL: 1
          OCI_TEST_PUSH: 1
          OCI_TEST_CONTENT_DISCOVERY: 1
          OCI_TEST_CONTENT_MANAGEMENT: 1
          OCI_HIDE_SKIPPED_WORKFLOWS: 0
          OCI_DEBUG: 0

      - name: Stop registry server
        if: always()
        run: |
          if [ -f /tmp/registry.pid ]; then
            kill $(cat /tmp/registry.pid) || true
          fi

      - name: Parse conformance test results
        if: always()
        id: oci-results
        run: |
          set -e

          # Parse the HTML report generated by the OCI conformance tests
          # The report contains a summary in the format: "74 passed" and "5 skipped"

          PASSED=$(grep -oP '\d+(?= passed)' report.html 2>/dev/null || echo "0")
          FAILED=$(grep -oP '\d+(?= failed)' report.html 2>/dev/null || echo "0")
          SKIPPED=$(grep -oP '\d+(?= skipped)' report.html 2>/dev/null || echo "0")

          # Calculate totals
          RAN_SPECS=$((PASSED + FAILED))
          TOTAL_SPECS=$((RAN_SPECS + SKIPPED))

          echo "passed=${PASSED}" >> $GITHUB_OUTPUT
          echo "failed=${FAILED}" >> $GITHUB_OUTPUT
          echo "skipped=${SKIPPED}" >> $GITHUB_OUTPUT
          echo "ran_specs=${RAN_SPECS}" >> $GITHUB_OUTPUT
          echo "total_specs=${TOTAL_SPECS}" >> $GITHUB_OUTPUT

          echo "✅ Parsed results from report.html"
          echo "Results: ${PASSED} passed, ${FAILED} failed, ${SKIPPED} skipped"

      - name: Generate test summary
        if: always()
        run: |
          echo "| Status | Test |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|------|" >> $GITHUB_STEP_SUMMARY

          # Cyclo test
          if [ "${{ steps.test-cyclo.outcome }}" == "success" ]; then
            echo "| ✅ Passed | **Cyclomatic Complexity** - No functions with complexity > 15 |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ❌ Failed | **Cyclomatic Complexity** - Some functions have high complexity |" >> $GITHUB_STEP_SUMMARY
          fi

          # Misspell test
          if [ "${{ steps.test-misspell.outcome }}" == "success" ]; then
            echo "| ✅ Passed | **Spell Checking** - No spelling errors found |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ❌ Failed | **Spell Checking** - Spelling errors detected |" >> $GITHUB_STEP_SUMMARY
          fi

          # Go test
          if [ "${{ steps.test-go.outcome }}" == "success" ]; then
            echo "| ✅ Passed | **Unit Tests** - All unit tests successful |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ❌ Failed | **Unit Tests** - Some unit tests failed |" >> $GITHUB_STEP_SUMMARY
          fi

          # OCI Distribution Spec conformance test
          if [ "${{ steps.oci-results.outputs.failed }}" == "0" ]; then
            echo "| ✅ Passed | **OCI Distribution Spec Conformance** - ${{ steps.oci-results.outputs.passed }} passed, ${{ steps.oci-results.outputs.skipped }} skipped |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ❌ Failed | **OCI Distribution Spec Conformance** - ${{ steps.oci-results.outputs.passed }} passed, ${{ steps.oci-results.outputs.skipped }} skipped, ${{ steps.oci-results.outputs.failed }} failed |" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "${{ steps.test-cyclo.outcome }}" != "success" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details><summary>Cyclomatic Complexity Report</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            cat test-cyclo.log >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "${{ steps.test-misspell.outcome }}" != "success" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details><summary>Spell Checking Report</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            cat test-misspell.log >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "${{ steps.test-go.outcome }}" != "success" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details><summary>Unit Tests Report</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            cat test-go.log >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Display coverage summary
        if: always()
        run: |
          if [ -f build/cover.txt ]; then
            COVERAGE=$(grep "total:" build/cover.txt | awk '{print $3}')
            if [ -n "$COVERAGE" ]; then
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "**Total Coverage: $COVERAGE**" >> $GITHUB_STEP_SUMMARY
            fi

            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details><summary>Coverage Report</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            cat build/cover.txt >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Display registry logs
        if: always()
        run: |
          if [ -f /tmp/registry.log ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details><summary>Registry Logs</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            cat /tmp/registry.log >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: test-results-${{ github.sha }}
          path: |
            test-cyclo.log
            test-misspell.log
            test-go.log
            build/cover.out
            build/cover.txt
            build/cover.html
          retention-days: 30
          if-no-files-found: ignore

      - name: Check if all tests passed
        if: always()
        run: |
          FAILED=0

          if [ "${{ steps.test-cyclo.outcome }}" != "success" ]; then
            echo "❌ Cyclomatic complexity test failed"
            FAILED=1
          fi

          if [ "${{ steps.test-misspell.outcome }}" != "success" ]; then
            echo "❌ Misspell test failed"
            FAILED=1
          fi

          if [ "${{ steps.test-go.outcome }}" != "success" ]; then
            echo "❌ Go unit tests failed"
            FAILED=1
          fi

          if [ "${{ steps.test-oci.outcome }}" != "success" ]; then
            echo "❌ OCI Distribution Spec Conformance test failed"
            FAILED=1
          fi

          if [ $FAILED -eq 1 ]; then
            echo ""
            echo "One or more tests failed. Check the summary above for details."
            exit 1
          else
            echo "✅ All tests passed successfully!"
          fi

  release:
    name: Release
    runs-on: ubuntu-latest
    needs:
      - tests
    if: |
      github.ref_type == 'tag'
    steps:
      - name: Validate version tag format
        run: |
          TAG_NAME="${{ github.ref_name }}"
          if [[ ! "$TAG_NAME" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "❌ Invalid tag format: $TAG_NAME"
            echo "Expected format: vX.Y.Z (e.g., v1.2.3)"
            exit 1
          fi
          echo "✅ Valid version tag: $TAG_NAME"

      - name: Checkout code
        uses: actions/checkout@v6
        with:
          lfs: true

      - name: Generate Repository summary
        run: |
          echo "## 🗂️ Repository summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Tag:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: "go.mod"
          cache: true

      - name: Build Artifacts
        id: artifacts-build
        run: make artifacts

      - name: Generate Artifacts summary
        if: always()
        run: |
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📦 Binary Artifacts summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Binary artifacts summary
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.artifacts-build.outcome }}" == "success" ]; then
            echo "**✅ Build Successful**" >> $GITHUB_STEP_SUMMARY
          else
            echo "**❌ Build Failed**" >> $GITHUB_STEP_SUMMARY
          fi
          if ls build/simple-registry_*.tar.gz 1> /dev/null 2>&1; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details><summary>Archives</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            ls -lh build/simple-registry_*.tar.gz | awk '{print $9, "(" $5 ")"}' >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi
          if ls build/simple-registry_*.sha256 1> /dev/null 2>&1; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details><summary>Checksums</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            cat build/simple-registry_*.sha256 >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for images
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ vars.DOCKERHUB_USERNAME }}/simple-registry
            ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable={{is_default_branch}}
          flavor: |
            latest=auto

      - name: Build and push Docker images
        id: docker-build
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: release-artifacts
          path: |
            build/simple-registry_*.tar.gz
            build/simple-registry_*.sha256
          if-no-files-found: error

      - name: Create Release
        id: create-release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/simple-registry_*.tar.gz
            build/simple-registry_*.sha256
          generate_release_notes: true
          draft: false
          prerelease: false
          body: |
            ## 🖥️ Platforms:

            - linux/amd64
            - linux/arm64

            ## 🐳 Container Images:

            - docker.io/${{ vars.DOCKERHUB_USERNAME }}/simple-registry:${{ github.ref_name }}
            - ghcr.io/${{ github.repository }}:${{ github.ref_name }}

            ## 📦 Binary Installation

            ```sh
            ARCH=$(uname -m | sed 's/x86_64/amd64/; s/aarch64/arm64/') && curl -L https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/simple-registry_${{ github.ref_name }}_linux-$ARCH.tar.gz | tar -xz simple-registry
            ```

      - name: Generate Release summary
        if: always()
        run: |
          if [ "${{ steps.create-release.outcome }}" == "success" ]; then
            echo "## 🚀 Release Published Successfully" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Release URL:** ${{ steps.create-release.outputs.url }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "## ❌ Release Publication Failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "The release could not be published. Check the logs for details." >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
